# Инструкции для работы с проектом

## Назначение и источники требований

Проект — адаптивное портфолио продуктового дизайнера Елены Сулоевой. Объём, заданный README: главная страница, отдельный кейс «Корпоративный транспорт» и общие завершающие блоки для каждой страницы кейса.

- Перед изменениями прочитай `readme` в корне (имя без расширения).
- Для чтения макетов, параметров слоёв и получения изображений используй MCP-сервер Figma, как указал пользователь.
- Файл Figma: `ZGvSZcFNYGG8ePhMb3iuk0`.
- [Десктопный макет](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2074-10207): узел `2074:10207`, размер фрейма 1440 × 3778 px.
- [Мобильный макет](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2346-4584): узел `2346:4584`, размер фрейма 422 × 4525 px.
- Оба макета проверены по скриншотам через MCP. Точные шрифты, цвета, отступы и размеры отдельных элементов нужно получать из слоёв перед реализацией; не считай визуальные оценки точными значениями.
- Если MCP недоступен, сообщи об ограничении; не выдавай предполагаемые параметры за проверенные.

### Макеты кейса и общих блоков

Все ссылки относятся к тому же файлу Figma. Размеры ниже получены через MCP; они описывают фреймы, а не фиксированные размеры HTML-страниц.

| Область | Десктоп | Мобильная версия |
| --- | --- | --- |
| Корпоративный транспорт | [1998:3750](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=1998-3750), 1440 × 8138 px | [2347:4584](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2347-4584), 390 × ≈9552 px |
| Другие проекты | [2485:6295](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2485-6295), 1440 × 564 px | [2405:16839](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2405-16839), 390 × 664 px |
| Сотрудничество и подвал | [2074:10477](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2074-10477), 1440 × 484 px | [2354:6800](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2354-6800), 390 × 452 px |

Новые фреймы проанализированы по метаданным и обзорным скриншотам MCP. Полные тексты и точные стили всех секций ещё не извлечены. Перед реализацией получи `get_design_context` нужных секций, предварительно прочитав руководство `figma-design-to-code`; не восстанавливай мелкий текст по обзорному скриншоту или обрезанным именам слоёв.

## Состояние репозитория и размещение файлов

Сайт реализован на HTML, CSS и JavaScript без сборки и зависимостей времени выполнения. Главная страница — `site/index.html`, стили — `site/styles.css`, поведение меню — `site/script.js`. Локальные шрифты подключены через `site/fonts.css`, файлы и лицензии находятся в `site/fonts/`. Источники изображений описаны в `pic/README.md`.

Пока реализована только главная. Страница кейса и карусель «Другие проекты» отсутствуют. Требования к ним ниже описывают предстоящую реализацию, а не готовые возможности сайта.

- Код сайта размещай в `site/`.
- Изображения размещай в `pic/` в корне репозитория, рядом с `site/`, как уже организовано в проекте.
- При настройке сервера или сборки обеспечь доступность изображений из `pic/`; проверь пути также в итоговом варианте сайта.
- Не назначай фреймворк или пакетный менеджер на основании кода, сгенерированного Figma. Сначала учитывай актуальную структуру проекта и требования задачи; выбирай минимально необходимый инструментарий.
- Локальный запуск из корня: `python -m http.server 8000 --bind 127.0.0.1`, адрес — `http://127.0.0.1:8000/site/`. Быстрые проверки: `node --check site/script.js` и `git diff --check`. При появлении сборки задокументируй её команды в `readme`.
- `.local/` содержит игнорируемые служебные инструменты и результаты проверок; не подключай эту папку к сайту и не коммить её.
- Сохраняй существующие пользовательские изменения. Не редактируй настройки IDE без необходимости для задачи.

## Структура главной страницы

Сохраняй порядок и содержание блоков из макетов:

1. Шапка: аватар и имя, ссылки «Обо мне», «Проекты», «Опыт работы», «Контакты», кнопка связи. На мобильном — компактная шапка с кнопкой меню.
2. Вступление: Telegram и почта, крупный заголовок о дизайнере, четыре направления работы — UX-исследования, User flows & wireframes, UI-дизайн, AI.
3. Проекты: «Транспортное приложение», «Сервис заказа корпоративного транспорта», «Платформа для поиска компании и активностей в путешествиях», «АРМ Кассира». Каждая карточка содержит категорию, описание, сведения о работе, ссылку на кейс и изображение.
4. Опыт в продуктовых и проектных командах и профильное обучение.
5. Контакты «Открыта к сотрудничеству», Telegram, почта и подвал.

Тексты, контакты, изображения и порядок карточек сверяй с Figma. Макеты остальных кейсов пока не заданы; не придумывай их содержание.

Сейчас элементы `.case-link` неактивны (`aria-disabled="true"`). При реализации локального кейса выбери понятное имя HTML-файла в `site/`, задокументируй его в `readme` и подключи настоящую ссылку на эту страницу. Предоставление макета позволяет создать локальный адрес; ждать внешнего URL для такого кейса не нужно. Не используй Figma-ссылку как адрес готового кейса. Для ещё не созданных страниц сохраняй явное неактивное состояние.

### Существующие изменения, которые нужно сохранить

- Hover карточок главной: плавный подъём, синяя рамка, тень и небольшое увеличение изображения. Эффект включён для устройств с точным указателем и hover; при `prefers-reduced-motion: reduce` перемещения отключены.
- Превью корпоративного транспорта на главной выбирается через `picture`: свыше 700 px — `pic/corporate-desktop.png` из [узла 2484:6293](https://www.figma.com/design/ZGvSZcFNYGG8ePhMb3iuk0/?node-id=2484-6293), до 700 px включительно — прежний `pic/corporate.png`.
- Эта замена относится к карточке главной. Иллюстрации внутри кейса и карусели сверяй с соответствующими фреймами отдельно.

## Страница кейса «Корпоративный транспорт»

Сохраняй последовательность разделов:

1. Общая шапка, ссылка «Назад на главную» и разделитель.
2. Обложка: название сервиса, роль и платформы, пометка о данных, композиция экранов приложения.
3. Контекст: один сервис для фиксированного и плавающего графика.
4. Продуктовая задача: два сценария в рамках одного продукта, переход от требований заказчика к сценариям и вклад дизайнера.
5. Предзаказ: гибкость для нерегулярного пользователя, экран, объяснение решения и последовательность заказа.
6. Закреплённый маршрут: назначенный маршрут и ближайший рейс, сравнение двух сценариев.
7. Карта как инструмент: местоположение пользователя, транспорта и остановки, релевантная информация.
8. QR и посадка: доступность кода перед посадкой, объяснение обновления, пользовательская и бизнес-ценность.
9. Бизнес-логика: предзаказы, данные о спросе, решение о закреплении маршрута и ценность для обеих сторон.
10. Edge cases: перенос и отмена рейса с соответствующими состояниями интерфейса.
11. «Другие проекты», затем «Открыта к сотрудничеству» и подвал из отдельных фреймов.

- Основные фреймы кейса заканчиваются разделом Edge cases. Отдельные завершающие блоки обязательны даже при их отсутствии внутри этих фреймов.
- Содержание экранов приложения — иллюстрации кейса, а не требование реализовать на сайте заказ транспорта, карты или QR-билеты.
- Не выдумывай результаты исследований, метрики, роль автора или продуктовые правила. Переноси подтверждённый текст, включая пометку о данных.
- На мобильном используй отдельный макет: порядок текста и иллюстраций, размеры экранов и сравнительные блоки могут отличаться от десктопа.
- В мобильных сравнительных блоках есть содержимое за границами видимой области. Сделай его доступным локальной прокруткой или управлением согласно контексту секции; не обрезай второй сценарий и не создавай горизонтальный скролл всей страницы.

## Общие блоки и навигация между страницами

### Другие проекты

- Это горизонтальная карусель с заголовком и кнопками «Предыдущий проект» / «Следующий проект» на обеих версиях.
- На десктопе карточка ориентировочно 680 × 300 px, текст слева, изображение справа, промежуток 24 px. На мобильном — 343 × 522 px, текст над изображением, промежуток 16 px. Это размеры из макета: адаптируй их к узким экранам без обрезания текста.
- Частично видимая следующая карточка относится к карусели. Ограничивай переполнение её областью; поддержи свайп/прокрутку и кнопки, учитывай границы списка и изменение ширины окна.
- Не добавляй автоматическое перелистывание: оно не задано. Учитывай уменьшение анимации и доступность управления с клавиатуры.
- Названия некоторых слоёв и метки карточек расходятся с видимыми проектами; порядок карточек между десктопным и мобильным примерами тоже различается. Сверяй содержимое по `get_design_context` и изображениям, не определяй проект только по имени слоя.
- По смыслу «Другие проекты» по умолчанию исключай открытый кейс из списка и сохраняй единый порядок остальных на разных ширинах. Это решение реализации, а не прямое требование README; явные указания пользователя о составе имеют приоритет.

### Сотрудничество и подвал

- Повторяй блок в конце каждого кейса после карусели. Видимый заголовок в обоих макетах — «Открыта к сотрудничеству».
- Контакты: `https://t.me/Lena_Suloeva` и `mailto:lensuloeva@yandex.ru`; подвал содержит имя, специализацию, «Портфолио · 2026» и синюю линию.
- На десктопе контакты стоят рядом, на мобильном — друг под другом. Переиспользуй стили главной там, где они соответствуют макету.
- Общим блокам назначай общие классы, стилям кейса — собственную область действия. Изменение кейса не должно ломать `.project` и другие компоненты главной.
- Ссылка назад ведёт на главную. Ссылки шапки на её разделы должны содержать путь к главной и якорь, например `./index.html#projects` для HTML-файла рядом с `index.html`.
- Не подключай JS, который без проверки обращается к отсутствующим DOM-элементам. Проверь работу общего меню на каждой странице; основное содержимое и ссылки должны оставаться доступны без JavaScript.

## Вёрстка и адаптивность

- Сохраняй тёмную палитру, синие акценты, визуальную иерархию, скругления и тонкие границы из макета.
- На десктопе направления работы стоят в ряд; карточки проектов разделены на текст слева и изображение справа; опыт и контакты используют колонки.
- На мобильном направления работы, содержимое проектов, опыт и контакты выстраиваются вертикально. Изображение проекта располагается после текста.
- Реализуй плавную адаптацию между макетами. Ширины 1440 и 422 px — контрольные размеры, а не обязательные точки переключения CSS.
- Для нового кейса и завершающих блоков мобильная контрольная ширина — 390 px. У главной внешний фрейм 422 px содержит Body шириной 390 px. Проверяй обе ширины; не переноси пустую область фрейма в вёрстку.
- Высота страницы определяется содержимым: не фиксируй её по высоте фрейма Figma.
- Используй общие стили для повторяющихся карточек, кнопок и отступов. Предпочитай Flexbox/Grid; не воспроизводи всю страницу абсолютными координатами.
- Получай исходные изображения и иконки через Figma MCP и сохраняй локально в `pic/`. Временные URL MCP не используй как постоянные адреса ресурсов сайта.

## Типографика и доступность

- Обязательное требование README: не оставляй висячие предлоги в конце строки. Связывай предлог со следующим словом неразрывным пробелом (`\u00a0` или `&nbsp;` в HTML) и проверяй результат на разных ширинах.
- Не запрещай перенос всего абзаца ради одного предлога. Не расставляй повсюду `<br>` для имитации скриншота.
- Используй семантические разделы, один основной `h1` и последовательную иерархию заголовков.
- Ссылки должны быть ссылками, действия — кнопками. Обеспечь видимый фокус и управление с клавиатуры.
- Кнопке мобильного меню нужны доступное имя и состояние `aria-expanded`; открытие, закрытие и порядок фокуса должны работать.
- Содержательным изображениям добавляй осмысленный `alt`, декоративным — пустой.

## Проверка изменений

- При изменении только документации проверь её соответствие README, макетам и актуальному дереву файлов; запуск тестов не требуется.
- При реализации страницы сравни результат с обоими макетами на ширинах 1440 и 422 px, а также проверь промежуточную ширину и узкий экран 320–375 px.
- Проверь отсутствие горизонтального скролла, обрезанного текста, висячих предлогов и искажений изображений.
- Проверь меню, переходы к разделам, контактные ссылки, клавиатурную навигацию и видимый фокус.
- Для кейса проверь полный путь: главная → кейс → главная, ссылки шапки, наличие обоих завершающих блоков, карусель до последней карточки и обратно, доступность всех сравнительных экранов на мобильном.
- После изменения общих CSS/JS проверь затронутые блоки на главной и в кейсе, включая hover, `prefers-reduced-motion` и выбор мобильного/десктопного изображения корпоративного транспорта.
- Проверь загрузку локальных ресурсов, отсутствие ошибок в консоли и работу итоговой сборки, если она есть.
- Запускай существующие проверки, относящиеся к изменению. Не заявляй о пройденных тестах или визуальной проверке, если они не выполнялись.
- В отчёте кратко укажи, что изменено, что проверено и какие вопросы остались нерешёнными.
